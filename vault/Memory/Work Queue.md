---
tags: [memory, queue]
updated: 2026-09-26
---
# Work Queue

The hourly Routine works the **top unchecked items that aren't marked (owner)**, up to 3 per run (about one every 20 minutes). See [[Hourly Workflow]].
Add new items with an owner role. Keep the list in priority order.

## Queue (priority order)
- (empty — see [[Active Context]] for what's waiting on the owner)

## Waiting on the owner (runs skip these)
- [ ] (owner) Play-test Sproutling Isles in Studio; answer [[Decisions]] (Sproutling #1–4 block the next build; #20 product line).
- [ ] (owner) Create the 13 Robux items and share the IDs.
- [ ] (owner) Create a "Summon Spooky Fog" dev product (proposed 149 R$) and share the ID for the Hollow Harvest event.
- [ ] (owner) Studio playtest of the Hollow Harvest Halloween event before 17 Oct (fast-forward time to check the Fog schedule, lanterns, stall, skin, and end-of-event conversion) — Studio isn't available in the sandbox, so this needs the owner.
- [ ] (owner) Trading agent: OANDA setup (add `api-fxpractice.oanda.com` / `api-fxtrade.oanda.com` to network access, set `OANDA_ENV` and `OANDA_ACCOUNT_ID`).
- [ ] (owner) Clip Studio, Family Hub: say what's next (deploy? more features?).
- [ ] (owner) StarNet install and Gumroad account (see [[StarNet Automation]], [[Gumroad Digital Products]]).

## Needs you: costs money (runs add items here and send a push notification)
- (none yet)

## Run log (newest first; one line per hourly run)
- 2026-09-26: everything blocked. Checked the other dormant projects too (Trading Agent, Gumroad Digital Products, StarNet Automation) in case something had quietly unblocked — all still genuinely owner-gated (Gumroad needs the owner's line pick before any product work per its own task list; StarNet is pending Decision #21; Trading Agent needs OANDA). Already used the last empty-queue window (previous run) for a substantive QA re-review that found and fixed a real bug — not repeating that same move without new material to review, per [[Hourly Workflow]]'s "nothing to do → one line, stop" path. CI green on both branches. Stopping.
- 2026-09-26: **2 tasks this run.** (1) Work Queue was empty and the other dormant projects (Bybit Trading Bot, Family Hub) also had nothing unblocked — rather than manufacture a speculative new project, delegated a whole-day QA re-review of today's substantial Sproutling Isles changes (EventTimeOffset, Codes system, Founding Gardener/Frostbloom teaser, the 3-part audit-economy tool) to qa-tester, checking for drift/interaction bugs between individually-reviewed commits rather than re-litigating each one. Found a real, new gap I independently verified myself: `Events.now()` (built for Hollow Harvest's `Config.EventTimeOffset` hook) covers all 11 Hollow Harvest call sites, but the Founding Gardener and Frostbloom-teaser window checks added in a *later* commit the same day (`35ab411`) still called raw `os.time()`, so the Studio fast-forward hook had zero effect on them — a genuine interaction bug between two individually-correct commits. Everything else (tooling baseline, audit-economy's own output hand-checked against the actual source files, Codes.list/audit-economy agreement, 5 spot-checked 2026-09-23 fixes) passed clean, no other drift found. (2) Delegated the one-line fix to roblox-engineer: `init.server.luau`'s `local now = os.time()` → `local now = Events.now()`, right before both window checks. Pushed `75c0dc2`. I reviewed the diff myself — exactly the one line, no-op in production (Events.now() == os.time() outside Studio), confirmed the other reads of the same `now` local (shield expiry, joinedAt) are unaffected/expected. selene/rojo/lune clean, 1241 parts unchanged, CI confirmed green (run #108). Stopping at 2 tasks.
- 2026-09-26: roblox-engineer extended `tools/audit-economy.luau` with a third report section, Dev product Dew-per-Robux rates (`e10e86b`, branch `claude/roblox-popular-game-trends-6u7sie`): parses each `Products.DevProducts` description for a flat or minimum-floor Dew amount and prints Dew/Robux sorted best-to-worst, explicitly skipping (with reasons) products/passes that grant no lump Dew amount rather than guessing. I reviewed the diff and hand-verified all 3 rated products' math myself (StarterPack 5000/49=102.0, DewLarge 20000/249=80.3, DewSmall 500/29=17.2 — all correct) and confirmed the min-floor caveat is honestly worded (a floor, not an income estimate). selene/rojo/lune clean, 1241 parts unchanged, CI confirmed green (run #106). This closes both follow-up tasks queued from the original audit-economy build — **the Work Queue is now empty** (0 unblocked items). 1 task this run; stopping.
- 2026-09-26: roblox-engineer extended `tools/audit-economy.luau` to check `Codes.luau`'s seed rewards against `Products.luau`'s paid catalogue (`f9e0031`, branch `claude/roblox-popular-game-trends-6u7sie`): finds the cheapest Rare+ seed grantable by any paid dev product (parsed from `Products.luau`'s free-text descriptions — currently matches "Mushroomph" in the Starter Pack, 120s payback — with a small hardcoded fallback if the text-parse ever finds nothing), then warns if any free code's seed reward pays back slower than that floor. I reviewed the diff myself: the logic correctly reproduces the exact GROWINGSTRONG comparison (verified by the agent's own before/after sanity test, which I confirmed left zero diff on `Codes.luau` — the reintroduced bug used to test the warning was never staged or committed), current `Codes.list` reports clean ("No free-tier code rewards exceed..."), selene/rojo/lune clean, 1241 parts unchanged, CI green (run #104). 1 task this run; stopping.
- 2026-09-26: Work Queue was empty (0 unblocked items; everything else owner-blocked) — per [[Hourly Workflow]]'s "new project" rule, considered starting one. Chose not to formalize it as a capital-P new project in `CLAUDE.md`/[[00 Home]] (that machinery fits a genuinely separate product, not an in-repo dev tool that has to live inside `roblox/sproutling-isles/tools/` to work via relative requires) — instead built a small, well-scoped addition to the existing Sproutling Isles project directly (no agent needed): `tools/audit-economy.luau`, a Lune script that prints every species' payback time grouped by rarity, so the earn-rate curve can be checked with one command instead of by hand. This is the exact check that caught the `GROWINGSTRONG` balance bug 2 runs ago. Had to work around Lune not exposing `Color3` as a real global (Species.luau evaluates `Color3.fromRGB(...)` at module load time) — tested and confirmed that setting a bare global before `require`ing a module works, since Lune shares one global environment across `require`d scripts. Ran it: output is a clean, monotonic curve (Common 10s → Secret 2083s) matching every prior hand-check in [[Sproutling Isles - QA Review]]. selene clean, rojo/lune bake unaffected (1241 parts, tool lives outside the Rojo tree). Pushed `d1dcc75` to `claude/roblox-popular-game-trends-6u7sie`; CI confirmed green (run #100). Added 2 follow-up tasks (extend it to auto-check Codes.luau and Products.luau against the curve) so the queue has real, unblocked work for future runs. 1 task this run (on top of the CI check); stopping.
- 2026-09-26: roblox-engineer implemented the final Week 5 code list into `Codes.luau`'s `Codes.list` (`da077ea`, branch `claude/roblox-popular-game-trends-6u7sie`): all 5 codes (`SPROUT1K` 1,000 Dew, `MISSEDBLOOM` 500 Dew + 1 mossbun seed, `LUCKYGARDEN` 5-min Luck Boost, `GROWINGSTRONG` 250 Dew + 1 mushroomph seed, `SEEDSTASH` 2 mossbun seeds). I reviewed the diff myself: matches the vault's final table exactly, including the `mushroomph` swap from this run's earlier `GROWINGSTRONG` fix — no `lilypadger` leaked back in. selene/rojo/lune clean, 1241 parts unchanged. CI confirmed green (run #96). **The Work Queue is now empty** (0 unblocked items) — per [[Hourly Workflow]], next run should consider starting a new project (max one/day, original, owner still has final say) since fewer than 3 unblocked items remain. 1 task this run (following the game-designer resolution); stopping.
- 2026-09-26: resolved last run's `GROWINGSTRONG` balance flag directly (no agent — a simple pick between two already-scoped options). Swapped the reward from `lilypadger` (Rare, 180s payback, priced above the Starter Pack's own Rare seed) to `mushroomph` (Rare, 120s payback, matches the Starter Pack's own seed, no longer priced above it). Kept the code evergreen rather than adding per-code expiry logic `CodesService.luau` doesn't have. All 5 Week 5 codes are now unflagged; merged the two split queue tasks back into one roblox-engineer implementation task.
- 2026-09-26: reviewed live-ops-manager's Week 5 code list proposal (below) before recording it done, per this session's standing discipline — even for vault-only design tasks, not just code. Checked the actual species data in `Species.luau` on the roblox branch and found a real proportionality issue in `GROWINGSTRONG`: it proposes a free, evergreen, one-time Rare seed (`lilypadger`, 180s payback: 18,000 price ÷ 100 dewPerSecond) that's priced *above* the paid Starter Pack's own Rare seed (`mushroomph`, 120s payback: 6,000 ÷ 50) — the one code on the list that's a bigger giveaway than a paid item, not smaller, unlike the other 4. Added a reviewer-flag callout directly in the vault note, split the queue task into "game-designer confirms/adjusts `GROWINGSTRONG`" (blocking just that one code) and "roblox-engineer implements the other 4 now, `GROWINGSTRONG` once resolved." Didn't touch the design myself — the exact anti-inflation ceiling for a free Rare seed is a game-designer call. 1 task this run; stopping.
- 2026-09-26: live-ops-manager proposed the real Week 5 code list, vault-only (no `.luau` access), to replace the `SPROUT1K`-only placeholder in `Codes.luau`. Read the Week 5 section, the store table (Dew Pouch, Dew Barrel, Luck Boost, Starter Pack) and the Hollow Harvest/Frostbloom limited-species Dew-reference prices to size rewards consistently with the existing economy. Proposed 5 codes, written into [[Sproutling Isles - Launch & Live Ops]] as a new "Week 5 code list" subsection: `SPROUT1K` (kept, 1,000 Dew, matches the existing announcement copy), `MISSEDBLOOM` (1 Common seed + 500 Dew, catch-up for Week 2-4 joiners), `LUCKYGARDEN` (5-min Luck Boost, matches the shortest planned ad-tier reward so it never beats the free ad path), `GROWINGSTRONG` (1 Rare seed + 250 Dew, a community-milestone thank-you — publish timing flagged as an owner marketing call, not a monetization one, since the reward needs no approval), `SEEDSTASH` (2 Common seeds, no Dew, for late/alt-account onboarding). All free-tier only (Dew, Common/Rare seeds already in the base roster, Luck Boost ≤ 15 min) — no skins, gamepasses or dev-product-gated perks, per the house rule and the existing `Codes.luau` comment. Every code tied to a specific 28-day retention window and KPI in the table. Ticked the queue item and added a new roblox-engineer task to implement the table into `Codes.list`. Did not touch any `.luau` file. 1 task this run; stopping.
- 2026-09-26: roblox-engineer built the Founding Gardener badge flag + Frostbloom Week 8 login tracker (`35ab411`, branch `claude/roblox-popular-game-trends-6u7sie`): new `Events.FoundingGardenerWindow`/`FrostbloomTeaserWindow` UTC windows, `Config.FoundingGardenerBadgeId = 0` placeholder (same "id=0 until created" convention as `Products.luau`), a new pcall-wrapped `Badges.luau` facade, and `data.foundingGardener`/`data.frostbloomTeaser` save fields set in `init.server.luau` on join. Deliberately scoped narrowly — no title/nameplate UI, and no Frostbloom Advent Gift system (that's unbuilt and not queued); this is a data-only tracker for a future task to consume. I reviewed the full diff myself and caught a real (if minor) error the agent's own report didn't: the code comment for the Frostbloom window's end timestamp said "Thu 11 Dec 2026" but 11 Dec 2026 is actually a **Friday** — verified independently with `date -u`. The timestamp value itself (1797033540) was already correct, only the human-readable label was wrong. Fixed directly (`bb7f395`), pushed, CI check in progress at time of writing. selene clean on both commits, rojo/lune unchanged (1241 parts, no map geometry touched, no logic touched by the comment fix). 1 task this run; stopping.
- 2026-09-26: roblox-engineer built the Week 5 Codes redemption system (`63c3100`, branch `claude/roblox-popular-game-trends-6u7sie`): new `Codes.luau` (reward table, `SPROUT1K = 1000 Dew` as the placeholder demo code from the announcement copy) + `CodesService.luau` (validation, one-time-per-player tracking via `session.data.codes`, reward granting, toasts, a 1s anti-spam cooldown matching `PondService`'s pattern), a top-left Codes button/panel on the client, and small wiring additions (`Net.RedeemCode`, `State.codeReadyAt`, `DataService` default data, an `Analytics` reason-prefix entry). I reviewed the full diff myself: Dew/seed grants reuse the existing `State.addDew`/`addSeeds` helpers, the Luck Boost branch matches the QA-confirmed M5 pattern (sets both the in-memory and saved `luckUntil` fields), one-time gating is set before granting (crash-safe), analytics call shape matches `Analytics.custom`'s signature, and the new UI button doesn't overlap the existing event panel above it. selene/rojo/lune all clean, 1241 parts unchanged, CI green (run #89). Only the demo code exists so far — added a follow-up task for live-ops-manager to write the real Week 5 code list before 7 Nov (not urgent, ~6 weeks out). 1 task this run; stopping.
- 2026-09-26: roblox-engineer built the `Config.EventTimeOffset` Studio test hook (`04a3211`, branch `claude/roblox-popular-game-trends-6u7sie`): added the Config flag, an `Events.now()` helper gated on `RunService:IsStudio()`, and swapped all 11 call sites listed in last run's QA spec (9 in `EventService.luau`, 1 each in `PlotService.luau`/`PondService.luau`). I reviewed the actual diff myself (not just the report): all 11 sites confirmed swapped, 0 `os.time()` left in `EventService.luau`, unrelated timers in the other two files correctly untouched. selene/rojo/lune all clean, 1241 parts unchanged, CI green. This closes the gap that was blocking all pre-17-Oct Studio testing of Hollow Harvest — the full phase-by-phase test plan in [[Sproutling Isles - QA Review]] can now actually be run once the owner has Studio access. 1 task this run; stopping (task took most of the run).
- 2026-09-26: qa-tester did the Hollow Harvest QA pass — found a real, previously-flagged-but-forgotten gap: `Config.EventTimeOffset` (an existing older task already called for it) was never actually built during the Halloween event work, so nobody can Studio-test any of Hollow Harvest before mid-October. Wrote a precise fix spec (11 call sites) and a full test plan ready to run once it lands. Starter Pack (m15) confirmed unchanged, not a regression. All 11 onboarding analytics guards confirmed correct, no double-fire risk. I spot-checked several of the file:line claims against the actual code — all accurate. Added the EventTimeOffset build as the new top queue item.
- 2026-09-26: did the "game-designer: confirm Frostbloom numbers" task directly (read-only verification, no agent needed). Re-checked all 3 species' payback arithmetic (correct) and their placement against the game's curve (agree, no changes). Also checked the Day-1-free-vs-Stall-sale interaction (intentional, not a bug — the free grant only reaches players who claim that specific day) and verified `Config.PlotCount = 8`/`SoilTilesPerPlot = 12` in code to sanity-check the Snowman tier thresholds are the right order of magnitude (left flagged for live tuning, same as the earn-rate estimates).
- 2026-09-26: live-ops-manager wrote the full Frostbloom Festival (winter event) spec — goal, player flow, weather/currency rules, 3 limited species with computed payback numbers, a 13-day Advent Gift calendar, a new "Snowman" per-server co-op mechanic, the Frosted Greenhouse skin, Robux proposals, save shape, edge cases, UI text, KPIs, and a build list flagging that `EventService.luau` likely needs a small refactor for 2 concurrent events. Verified the species' payback arithmetic and the Winter Bundle discount math myself — both check out exactly, and the bundle correctly reuses the existing Harvest Bundle precedent's prices. Written without code-branch access (vault-only), so the exact file shapes are flagged as inferred for roblox-engineer to confirm at build time. Added game-designer confirmation + 3 more unblocked tasks to the queue so it isn't empty next run. 1 task this run; stopping.
- 2026-09-26: roblox-engineer built the full Hollow Harvest Halloween event (`6f59f2f`) — Spooky Fog, Wisp Lanterns, 3 limited species, Lantern Stall, plot skin, end-of-event conversion, plus the analytics it unlocked (10 files, ~430-line new EventService.luau). Reviewed the whole diff line-by-line myself (dates hand-verified against `date -u`, cross-module calls checked, reconcile-backfill checked) and found one real bug: the Dew conversion formula dropped the berries-count multiplier entirely, so every player got the same flat payout regardless of balance. Fixed directly (`b8b34b4`), re-validated, CI green. 1 task this run (large scope); stopping.
- 2026-09-26: did the "game-designer: m11 + Halloween numbers" task directly (no agent needed — read-only analysis + a small, well-understood code fix). Confirmed the Halloween species' payback numbers against the base game's curve (all consistent). Diagnosed and fixed QA m11 (pond Rim walkable under the water surface) myself: `Rim.CanCollide = false`, `397cc4e`, validated clean, CI green. Only unblocked item was Halloween-event-adjacent, so did just this 1 task and stopped rather than jump to the bigger, deadline-sensitive Hollow Harvest build without more runway.
- 2026-09-26: roblox-engineer wired Sproutling Isles analytics recurring funnels (Store/Snatch/Rebirth) + custom events (Hatch, Snatch*, GateLock, Weather*, Rebirth, DailyClaim, PondCatch, StorageFull/NoSeeds, SessionEnd), `e432cfd`. EventShop/Ad funnels and Store steps 1-3 deliberately skipped (dependencies don't exist yet). I reviewed the full diff before recording — no bugs found this time (unlike last run's leaderboard bug). 2 tasks this run (CI port + funnels/events); stopping.
- 2026-09-26: **CI-first task.** The same pre-existing S3-mocking CI bug fixed on `main` last run was still red on `claude/roblox-popular-game-trends-6u7sie` (that branch forked before the fix, and also carries the Clip Studio code). Ported the identical fix (`c5e7884`); 30/30 tests pass locally. See [[Clip Studio]].
- 2026-09-26: roblox-engineer built Sproutling Isles leaderboards (cross-server income + rarest hatch, `21c68c3`). Reviewing the diff myself before recording found a real display bug (TextLabel nested wrong for the lookup code to ever find it — boards would have stayed on "Loading..." forever); fixed and re-validated directly (`6aece41`). 2 tasks this run (CI fix + leaderboards); stopping given the CI investigation's extra time.
- 2026-09-26: **CI-first task.** `main`'s CI had been red since 2026-09-23 (every push failing `storage.test.js`'s presigned-URL test — a mocking bug in `src/services/storage/s3.js`, unrelated to any of this session's vault-only pushes). Found the exact fix already on `claude/hourly-project-processing-ejbqs4` and ported it to `main` (`fd327a0`); 30/30 tests pass locally. See [[Clip Studio]] for detail.
- 2026-09-26: market-researcher re-checked Roblox rewarded-ads eligibility, Ads Manager pricing and the maturity questionnaire against Roblox's creator-docs GitHub mirror (direct create.roblox.com/devforum fetches are blocked in-sandbox). Found real corrections, not just re-confirmations: `ShowAdResult.ShowCompleted` not `Succeeded`, 3 extra eligibility requirements, Ads Manager bills as auto-bid/CPP not CPM/CPC, "new-creator ad credits" likely doesn't exist. Updated [[Sproutling Isles - Launch & Live Ops]] sections 1D/4/6 and ticked both tasks. 3/3 tasks done this run, stopping.
- 2026-09-26: market-researcher wrote the demand brief for all 3 Gumroad product lines (family printables, Roblox guides, spreadsheet/Notion templates) into [[Gumroad Digital Products]], with sources and an unverified-figures flag on anything from a blocked direct fetch. Deliberately did not pick one — [[Decisions]] #20 stays owner-pending.
- 2026-09-26: roblox-engineer built the Sproutling Isles analytics wrapper + 11-step onboarding funnel + economy source/sink logging (spec build notes 1-4), pushed `a136cba` to `claude/roblox-popular-game-trends-6u7sie`. selene/rojo/lune all clean. Not Studio-tested; recurring funnels + custom events queued as a follow-up.
- 2026-09-26: qa-tester re-reviewed the 2026-09-23 Sproutling Isles fixes in an isolated worktree (installed rojo/lune/selene fresh): tooling still clean (0 lint issues, build/bake unchanged), every claimed fix (B1, M1-M6, 14 minors) confirmed present and correct in code, still-open items confirmed still open. Updated [[Sproutling Isles - QA Review]] and ticked the task. 3/3 tasks done this run, stopping.
- 2026-09-26: qa-tester reviewed `claude/hourly-project-processing-ejbqs4` (Clip Studio) in an isolated worktree: 101+6+1 tests passed, CI steps reproduced except Docker build (sandbox limitation). 2 minor bugs found (no blockers). Added owner (merge decision) + app-engineer (fix) follow-ups in [[Clip Studio]].
- 2026-09-26: knowledge-keeper filled in [[Trading Agent]] from the repo's README/CHANGELOG (read-only, GitHub API, no clone). Added two owner follow-ups (OANDA setup; enable/skip the daily-paper.yml schedule).
- 2026-09-26 (setup test): worker session can clone and push to main.
- 2026-09-26: queue created from the project task lists (setup session).
